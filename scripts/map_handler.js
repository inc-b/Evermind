var mapZoom = 0;

var zoomIn = function(leftSelected){
	if (leftSelected){
		// Zoom in to left side of map
		console.log('left');
	} else {
		// Zoom in to right side of map
		console.log('right');
	}
};